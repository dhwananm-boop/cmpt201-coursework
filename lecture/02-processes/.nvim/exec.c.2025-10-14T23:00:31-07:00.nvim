#include <stdio.h>
#include <sys/types.h>
#include <unistd.h>

int main() {
  printf("Hello. This P1");
  fork();
  execl("/bin/echo", "/bin/echo", "Yes!", "No!", (char *) NULL);
  printf("This might not get executed");
}
